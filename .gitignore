node_modules/
dist/
.idea/
*.log
.DS_Store
.spec/
.claude/
.mcp.json

# Demo SQLite database (seeded on boot)
*.sqlite

# Playwright
test-results/
playwright-report/
blob-report/
# The logged-in browser state the e2e setup writes (holds a login cookie)
apps/kitchen-sink/e2e/.auth/

# Purchased template (Tailwind Plus license: not redistributable); the port lives in apps/docs
apps/docs/tailwind-plus-syntax/
