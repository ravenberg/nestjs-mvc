node_modules/
dist/
.idea/
*.log
.DS_Store
.spec/
.claude/
.mcp.json

# Demo SQLite database (seeded on boot)
*.sqlite

# The throwaway Vue app packages/core/test/vue.test.ts builds (removed after the run)
packages/core/test/.vue-app-*/

# Playwright
test-results/
playwright-report/
blob-report/
# The logged-in browser state the e2e setup writes per kitchen-sink app (holds a login cookie)
apps/kitchen-sink/*/.auth/

# Purchased template (Tailwind Plus license: not redistributable); the port lives in apps/docs
apps/docs/tailwind-plus-syntax/
