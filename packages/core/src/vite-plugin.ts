// Entry for `nestjs-mvc/vite`: imported from vite.config.ts, so it must not pull
// in the Nest layer.
export {
  nestjsMvc,
  CLIENT_ENTRY,
  SSR_ENTRY,
  PLUGIN_NAME,
  type NestjsMvcPluginOptions,
  type NestjsMvcPluginApi,
  type Framework,
} from './vite/plugin'
